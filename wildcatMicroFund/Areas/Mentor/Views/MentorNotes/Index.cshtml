@model IEnumerable<Note>

@{
    ViewData["Title"] = "Mentor Notes";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Application List</h2>
        </div>
    </div>
    <br /><br />

    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Note
                </th>
                <th>
                    Date
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model)
            {
                <tr>
                    <td width="50%">
                        @obj.NoteContent
                    </td>
                    <td width="40%">
                        @obj.CreatedDate
                    </td>
                    <td>
                        @*<div class="w-75 btn-group" role="group">
                            <a asp-controller="ReviewApplications" asp-action="Upsert" asp-route-id="@obj.AppStatId" asp-route-appId="@obj.Application.Id" asp-route-statId="@obj.Status.StatusID"
                           class="btn btn-primary mx-2"> <i class="bi bi-pencil-square"></i>Review</a>
                        </div>*@
                    </td>
                </tr>
            }
        </tbody>

    </table>
</div>